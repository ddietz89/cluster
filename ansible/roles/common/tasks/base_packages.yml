- name: base packages
  yum: name='{{ item }}' state=present
  with_items:
         - autoconf
         - automake
         - bind-utils
         - bzip2
         - bzip2-devel
         - bzip2-libs
         - epel-release
         - libtool
         - mariadb
         - mariadb-devel
         - mariadb-libs
         - mlocate
         - gcc
         - git
         - readline-devel
         - openssl-devel
         - perl
         - perl-ExtUtils-MakeMaker
         - pam-devel
         - rpm-build
         - vim
         - wget
         - zlib-devel
